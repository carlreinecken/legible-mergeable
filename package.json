{
  "name": "plain-array-crdt",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "description": "",
  "author": "Carl Reinecken",
  "main": "dist/legible-mergeable.js",
  "scripts": {
    "pretest": "npm run build",
    "test": "mocha",
    "lint": "standard",
    "clean": "rm -r dist && mkdir dist",
    "prebuild": "npm run lint && npm run clean",
    "build": "rollup src/index.js --file dist/legible-mergeable.js --format umd --name legible-mergeable",
    "postbuild": "terser dist/legible-mergeable.js > dist/legible-mergeable.min.js"
  },
  "standard": {
    "ignore": [
      "dist/"
    ],
    "env": {
      "mocha": true
    }
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.1.3",
    "rollup": "^2.26.10",
    "standard": "^14.3.4",
    "terser": "^5.3.0"
  }
}
