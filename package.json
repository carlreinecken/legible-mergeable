{
  "name": "plain-array-crdt",
  "private": true,
  "version": "0.1.0",
  "license": "MIT",
  "description": "",
  "author": "Carl Reinecken",
  "main": "dist/merge.js",
  "module": "src/merge.js",
  "scripts": {
    "pretest": "npm run build",
    "test": "mocha",
    "lint": "standard",
    "clean": "rm -r dist && mkdir dist",
    "prebuild": "npm run lint && npm run clean",
    "build": "rollup merge.js --file dist/merge.js --format umd --name merge",
    "postbuild": "terser dist/merge.js > dist/merge.min.js"
  },
  "standard": {
    "ignore": [
      "dist/"
    ],
    "env": {
      "mocha": true
    }
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^8.1.3",
    "rollup": "^2.26.10",
    "standard": "^14.3.4",
    "terser": "^5.3.0"
  }
}
